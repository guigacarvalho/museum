    <div class="ip-box">
        <div id="logo">
        </div>
        {{input type="text" value=email placeholder="Email (optional)" pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,4}$"}}<br>
        {{input type="text" value=zip placeholder="Zip Code"}}
		<br>
		{{input type="text" value=dob placeholder="Date of Birth"}}		
		<br>
		<div class="gender">
            <input type="radio" name="gender">
            <label class="radio" for="male">Male</label>
            <input type="radio" name="gender">
            <label class="radio" for="female">Female</label>
		</div>
		<p class="mailing-list">
			<input type="checkbox" name="mailing-list">
			<label class="checkbox" for="mailing-list">Receive Newsletters</label>
		</p>
		<button class="submit" type="button"  {{action 'save'}}>Submit</button>
	</div>